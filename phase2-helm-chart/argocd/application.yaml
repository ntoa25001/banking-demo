# DEPRECATED: Application đơn deploy tất cả — dễ gây restart/services khi sync
# Khuyến nghị: Dùng applications/ (per-service) — xem MIGRATE-TO-PER-SERVICE.md
# Phase 5: Postgres, Redis, Kong đã tách ra ns riêng — không deploy qua chart banking-demo
# Cách dùng: kubectl apply -f application.yaml -n argocd
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: banking-demo
  namespace: argocd
  labels:
    app.kubernetes.io/name: banking-demo
spec:
  project: banking-demo
  source:
    repoURL: https://github.com/ntoa25001/banking-demo.git  # ← Đổi thành repo của bạn
    targetRevision: main                                      # ← Branch hoặc tag
    path: phase2-helm-chart/banking-demo
    helm:
      releaseName: banking-demo
      valueFiles:
        - charts/common/values.yaml
        # Phase 5: Không còn postgres, redis, kong — dùng Phase 5 (ns postgres, redis, kong)
        - charts/auth-service/values.yaml
        - charts/account-service/values.yaml
        - charts/transfer-service/values.yaml
        - charts/notification-service/values.yaml
        - charts/frontend/values.yaml
        - charts/api-producer/values.yaml
      parameters:
        - name: postgres.enabled
          value: "false"
        - name: redis.enabled
          value: "false"
        - name: kong.enabled
          value: "false"
  destination:
    server: https://kubernetes.default.svc
    namespace: banking
  syncPolicy:
    # Auto sync mỗi khi ArgoCD phát hiện drift (tần suất = timeout.reconciliation trong argocd-cm)
    automated:
      selfHeal: true   # Tự động áp dụng thay đổi
      prune: false     # Không auto xóa resources — an toàn hơn
    syncOptions:
      - CreateNamespace=true
      # Chỉ apply resources thay đổi — rollout đúng service có values thay đổi, không động postgres/redis
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
